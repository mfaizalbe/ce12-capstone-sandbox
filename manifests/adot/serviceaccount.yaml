apiVersion: v1
kind: ServiceAccount
metadata:
  name: adot-collector
  annotations:
    # Hardcoded because ArgoCD's kustomize build has no envsubst step to resolve ${VAR}
    # placeholders, and this value never changes for this cluster.
    eks.amazonaws.com/role-arn: arn:aws:iam::255945442255:role/retail-store-grp5-adot-irsa
